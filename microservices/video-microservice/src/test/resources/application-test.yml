jpa:
  default:
    properties:
      hibernate:
        hbm2ddl:
          auto: create-drop
datasources:
  default:
    driver-class-name: org.mariadb.jdbc.Driver
    db-type: mariadb
    dialect: MYSQL
    url: jdbc:mariadb://localhost:3306/video
    username: todo
    password: todosecret  